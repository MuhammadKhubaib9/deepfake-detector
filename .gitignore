".env.*" 
".venv/" 
".venv/" 
".env" 
.venv/
venv/
__pycache__/
*.pyc

# Node / React frontend
frontend/node_modules/
frontend/dist/

# Local data (SQLite history) + test uploads
data/
uploads/

# Model weights: never in git - the loaders fetch them from the project's
# Hugging Face repo (Khubaib7/deepfake-models) when models/ is missing.
# models/

# Test benchmark media + artifacts (human-face eval set)
testing/
